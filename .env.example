# PropStrata-Engine Runtime Configuration
SECRET_KEY=django-insecure-propstrata-enterprise-secret-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0

# Server Settings
HOST=127.0.0.1
PORT=8097

# Database Configuration (Defaults to SQLite for instant local execution)
# DATABASE_URL=postgres://postgres:password@localhost:5432/propstrata_db

# CORS & Frontend Origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:8097

# Regional Defaults
DEFAULT_COUNTRY=KW
DEFAULT_CURRENCY=KWD
